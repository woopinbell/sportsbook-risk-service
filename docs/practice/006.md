> 생성물. 직접 수정 금지. 답: ../commits/006.md

# 006 · 동기 check API + 한도 관리 엔드포인트

## 개요

앞선 세 커밋(counter / limit / pattern)을 하나의 결정으로 합치는 커밋. betting-service가
베팅 접수 직전 동기로 부르는 `POST /internal/v1/risk/check`와, 운영자가 한도를 보고 바꾸는
관리 엔드포인트. 이 커밋이 서비스의 **read side(결정 경로)**를 닫는다 — Kafka leg(다음 커밋)만
남는다.

## 작업 순서

1. `RedisUserBetHistory` — 패턴 엔진이 읽는 history의 Redis 구현(read + write).
2. `RiskCheckService` — single-bet-max → stake 윈도우 → selections → 패턴 순서로 결정.
3. DTO(`RiskCheckRequest/Response`, `LimitUpdateRequest`, `UserLimitsResponse`) + 서비스 record.
4. `RiskCheckController` + `LimitController` + `RestExceptionHandler`.
5. `@WebMvcTest`→`standaloneSetup` MockMvc 테스트.

## 작업 내역

### 서비스 입출력 타입 — Command / Outcome / Rejection

**책임:** DTO와 분리된 서비스 계약. `RiskCheckCommand`는 평가 시각을 주입받아 결정론 유지.
`RiskCheckOutcome`은 세 팩토리(승인 / 한도 거절 / 패턴 BLOCK 거절)로만 생성 — 패턴 BLOCK은
`LimitRejection` 모양을 합성해 응답 매핑을 한 경로로 통일. record 헤더는 계약이므로 노출.

```java
public record RiskCheckCommand(
    String userId, String betId, Money stake, List<String> selectionIds, Instant now) {
  public RiskCheckCommand {
    // TODO: null 체크 + selectionIds 불변 복사
  }
}

public record LimitRejection(
    String reason, Currency currency, long current, long limit, long requested, String action) {}

public record RiskCheckOutcome(
    boolean approved, Optional<LimitRejection> rejection, List<PatternMatch> patternsFlagged) {

  public static RiskCheckOutcome approved(List<PatternMatch> flagged) {
    // TODO: approved=true, rejection=empty
  }
  public static RiskCheckOutcome rejectedByLimit(LimitRejection r, List<PatternMatch> matches) {
    // TODO: approved=false, rejection=Optional.of(r)
  }
  public static RiskCheckOutcome rejectedByPattern(PatternMatch block, List<PatternMatch> matches) {
    // TODO: "PATTERN_{RULE}" reason의 synthetic LimitRejection을 만들어 거절로
  }
}
```

### RiskCheckService — cheap-to-expensive 결정

**책임:** critical path. 싼 검사부터 해서 비싼 Redis 왕복 전에 거절할 수 있으면 거절한다.
순서: ① single-bet-max(메모리 비교, 왕복 0) → ② stake 일/주/월(각 sliding peek 1회 + resolver)
→ ③ selections-per-minute(count) → ④ 패턴 fold. 패턴은 BLOCK이어도 **끝까지** 돌아 SUSPECT/
REVIEW도 응답에 첨부. 매 호출을 `risk_check_latency_seconds` Timer로 감싸고, 위반/패턴마다
메트릭 카운터. 첫 위반에서 즉시 reject. (Kafka publish는 007에서 주입됨.)

```java
public RiskCheckOutcome check(RiskCheckCommand cmd) {
  // TODO: checkTimer.record(() -> doCheck(cmd))
}

private RiskCheckOutcome doCheck(RiskCheckCommand cmd) {
  // TODO 1: stake > policy.singleBetMax(currency)면 reject(SINGLE_BET_MAX_EXCEEDED).
  // TODO 2: STAKE_DAILY/WEEKLY/MONTHLY 각각 counter.currentSum(userKey, window, now) + stake가
  //         limitResolver.resolveUser(...) 초과면 reject({TYPE}_LIMIT_EXCEEDED).
  // TODO 3: selectionIds 수 + selections counter가 한도 초과면 reject(SELECTIONS_PER_MINUTE..., currency=null).
  // TODO 4: ruleEngine.evaluate(ctx)의 매 매치에 risk_pattern_flags_total++.
  //         BLOCK 매치가 있으면 rejectedByPattern, 없으면 approved(matches).
}

private RiskCheckOutcome reject(LimitRejection rejection) {
  // TODO: risk_limit_violations_total{reason}++, rejectedByLimit(rejection, List.of())
}
```

### RedisUserBetHistory — 패턴 history의 Redis 구현

**책임:** `UserBetHistory`(read) + `UserBetHistoryWriter`(write)를 Redis sorted set 두 개로
구현. `:bets`(score=ms, member="{betId}|{amount}")가 countBetsBetween(ZCOUNT)·recentStakeAmounts를,
`:sel:{selectionId}`가 countSelectionBets를 받친다. 7일 TTL이 모든 V1 윈도우를 덮어 별도 trim
불필요. 핵심 함정: `recentStakeAmounts`는 ZRANGE REV(최신순)를 받아 **뒤집어 oldest-first** 계약.

```java
@Component
public class RedisUserBetHistory implements UserBetHistory, UserBetHistoryWriter {

  // TODO: BETS_KEY_PREFIX("history:user:") / :bets / :sel: / TTL(7일) / '|' 상수
  // 키: history:user:{userId}:bets  와  history:user:{userId}:sel:{selectionId}

  @Override
  public void recordBet(
      String userId, String betId, long stakeAmount, List<String> selectionIds, Instant now) {
    // TODO: null/음수 체크. score=now(ms). bets에 "{betId}|{amount}" ZADD + EXPIRE.
    //       각 selectionId의 :sel: 키에 betId ZADD + EXPIRE.
  }

  @Override
  public long countBetsBetween(String userId, Instant from, Instant to) {
    // TODO: bets 키에 ZCOUNT [from, to] (null이면 0)
  }

  @Override
  public List<Long> recentStakeAmounts(String userId, int lookback) {
    // TODO: lookback<=0이면 빈 리스트. reverseRange(0, lookback-1)로 최신 N. 각 member의 "|amount" 파싱
    //       (corruption은 skip). 마지막에 Collections.reverse로 oldest-first.
  }

  @Override
  public long countSelectionBets(String userId, String selectionId, Duration window, Instant now) {
    // TODO: :sel: 키에 ZCOUNT [now-window, now] (null이면 0)
  }
}
```

### REST 계층 — 컨트롤러 + DTO

**책임:** DTO(검증 애너테이션은 계약 — 노출)와 컨트롤러. check는 DTO→Command 변환 후
service 호출, outcome→DTO(승인이면 reason/limitInfo null). `LimitController`는 GET/PATCH/DELETE
+ **SELECTIONS_PER_MINUTE 통화 무관 정규화**(store/clear 시 KRW sentinel, GET 응답에선 currency
null). GET은 모든 (type×currency) 조합을 돌며 override 유무로 source 표기.

```java
// 계약(노출): RiskCheckRequest(@NotBlank userId/betId, @NotNull Money stake, @NotEmpty List<@NotBlank> selectionIds)
//            RiskCheckResponse(@JsonInclude NON_NULL: approved, rejectionReason, limitInfo, patternsFlagged)
//            LimitUpdateRequest(@NotNull LimitType, Currency nullable, @PositiveOrZero amount)
//            UserLimitsResponse(userId, List<Entry>) / Entry(limitType, currency, value, source=OVERRIDE|POLICY)

@RestController @RequestMapping("/internal/v1/risk")
public class RiskCheckController {
  @PostMapping("/check")
  public RiskCheckResponse check(@Valid @RequestBody RiskCheckRequest request) {
    // TODO: request → RiskCheckCommand(clock.instant() 주입) → service.check → toResponse
  }
  // private toResponse(outcome): approved면 reason/limitInfo null + flags, 아니면 rejection 펼치기
}

// LimitController: effectiveCurrency(type, requested) — SELECTIONS는 KRW sentinel, 그 외 null이면 예외.
//   GET: LimitType.values() × Currency.values() 순회, override.orElseGet(resolver) + source.
```

### RestExceptionHandler — RFC 7807

**책임:** 모든 미처리 예외를 shared-protocol `ProblemDetail`(RFC 7807, `application/problem+json`)로
통일 — 다른 sportsbook 서비스와 wire 모양 일치(ADR-0004). validation→VALIDATION_FAILED,
NoHandler→404, 그 외→INTERNAL_ERROR.

```java
@RestControllerAdvice
public class RestExceptionHandler {
  // @ExceptionHandler(MethodArgumentNotValidException / IllegalArgumentException) → VALIDATION_FAILED
  // @ExceptionHandler(NoHandlerFoundException) → 404 NOT_FOUND ProblemDetail
  // @ExceptionHandler(Exception) → INTERNAL_ERROR

  private static ResponseEntity<ProblemDetail> problem(ErrorCode code, String detail) {
    // TODO: code.toProblemDetail(detail)을 code.httpStatus() + problem+json content-type으로
  }
}
```

### 두 가지 Spring MVC 마찰

**책임:** 통합 중 두 함정을 진단·수정한다. (1) `@WebMvcTest`가 application의 `clock` @Bean과
테스트 clock으로 `BeanDefinitionOverrideException` → 어떤 MockMvc 조립 방식이 컨텍스트를 안 띄워
충돌을 없애나? (2) `GET /limits/{userId}`가 전부 400(`Name for argument ... not available ...
'-parameters' flag`) → 무엇을 켜야 PathVariable 이름이 바이트코드에 보존되나?

```xml
<properties>
  <!-- TODO: PathVariable 이름 해석을 위해 켜야 하는 컴파일 프로퍼티 -->
</properties>
```

```text
TODO: 컨트롤러 테스트를 어떤 MockMvc 빌더로 조립했나? (컨텍스트 미기동 → Clock 빈 충돌 회피)
```

### 테스트

**책임:** `RiskCheckServiceTest`(Mockito), `RedisUserBetHistoryTest`(Testcontainers),
컨트롤러 2종(standaloneSetup MockMvc). 누적 67.

```java
// TODO: 검사 순서별 reject 분기, history 왕복(oldest-first 포함), MockMvc로 check/limits 엔드포인트.
```

## 검증

- `./mvnw test -Dtest=RiskCheckServiceTest,RiskCheckControllerTest,LimitControllerTest` → green.
- `./mvnw test -Dtest=RedisUserBetHistoryTest` (Docker 필요) → green.
- 감각 점검: stake가 single-bet-max 초과면 Redis 왕복 없이 거절되는가. 한도 임박 + BLOCK 패턴이
  동시일 때 응답에 둘 다(rejection + flags) 실리는가. `recentStakeAmounts`가 oldest-first인가.
  `GET /internal/v1/risk/limits/{userId}`가 400이 아니라 200을 주는가(-parameters).
